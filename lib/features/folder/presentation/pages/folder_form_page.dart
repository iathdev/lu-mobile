import 'package:flutter/material.dart';

class FolderFormPage extends StatelessWidget {
  const FolderFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FolderFormPage')),
      body: const Center(child: Text('FolderFormPage — TODO')),
    );
  }
}

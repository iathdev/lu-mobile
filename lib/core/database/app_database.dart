// TODO: Enable drift codegen later with `flutter pub run build_runner build`
// For now, this is a stub so the app compiles and runs without codegen.

/// Placeholder database class.
/// Will be replaced with drift-generated database after running build_runner.
class AppDatabase {
  // Singleton
  static final AppDatabase _instance = AppDatabase._();
  factory AppDatabase() => _instance;
  AppDatabase._();

  Future<void> clearAllCache() async {
    // TODO: implement with drift
  }
}
